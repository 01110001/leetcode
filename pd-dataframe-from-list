import pandas as pd

def createDataframe(student_data: List[List[int]]) -> pd.DataFrame:
    # Create a DataFrame using student_data
    df = pd.DataFrame(student_data, columns=['student_id', 'age'])
    return df
